# Server Configuration
PORT=3333
HOST=0.0.0.0

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
SUPABASE_BUCKET=profiles

# Metadata Provider
# Escolha qual implementação usar: "supabase" (padrão) ou "prisma"
METADATA_PROVIDER=supabase

# Database Configuration (para Prisma)
# IMPORTANTE: Migrations precisam da conexão DIRETA (porta 5432)
# Connection Pooler (porta 6543) NÃO funciona com migrations!

# Para MIGRATIONS (usar no desenvolvimento):
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres

# Para PRODUÇÃO com Connection Pooler (opcional):
# DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-sa-east-1.pooler.supabase.com:6543/postgres

# Security
BEARER_TOKEN=mock-token
ENCRYPTION_KEY=your-super-secret-encryption-key-min-32-chars

# Como obter o DATABASE_URL:
# 1. Acesse seu projeto no Supabase Dashboard
# 2. Vá em Settings > Database
# 3. Em "Connection string", selecione "URI" (não "Session pooler")
# 4. Copie a string e substitua [YOUR-PASSWORD] pela senha do banco

# Note: Gerar um chave segura:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"